# CAT Translate Plugin - 开发进度与规划

## 📅 项目概述

本项目旨在开发一个基于 Markdown 的辅助翻译工具（CAT），支持双栏对照翻译、元数据管理及项目化管理。

## 🚀 开发进度 (Roadmap)

### Phase 1: 基础框架与界面 (已完成)

-   [x] **UI 初始化**：实现点击按钮新建/打开翻译主窗口的功能。

### Phase 2: 核心数据流与分割 (当前重点)

-   [x] **文档分割触发器**
    -   点击按钮触发对当前 Markdown 文档的解析。
        -   如果当前未打开文档，弹窗提示用户先打开需要分割的 Markdown 文件。
    -   检查当源文档目录下是否存插件元数据文件夹`.catdata`
        -   如果不存在，则新建文件夹。
    -   检查是否存在对应的元数据文件`{源文件名}.json`。
        -   如果存在，则读取并加载已有的元数据。
        -   如果不存在，则调用分割算法初始化分割元数据并保存。
    -   允许用户选择重新分割当前文档并更新元数据。

-   [x] **Markdown文档分割**：
    -   一个Markdown文件对应一个单独的`TranslationData`对象。
        -   `TranslationData` 包含多个 `Block`，每个 `Block` 代表文档中的一行（包括空行）。
		-   每个 `Block` 包含多个 `Segment`，每个 `Segment` 包含单个以一个或多个分句符号`.?!。？！…`结尾的句子。
	-   分割逻辑：
        -   将每一行拆解为 `Prefix` (Markdown 标记/前缀) 和 `Content` (正文)。
            -   支持常见Markdown语法，包括`#`标题、下划线标题、无序列表、有序列表、脚注、链接等。
        -   正文再按照标点符号进行拆分。
        -   行内 Markdown 语法（如加粗/斜体）标记视为纯文本，交给译者处理。
    -   分割算法输出 `TranslationData` 并保存至 `.catdata/{源文件名}.json`。

### Phase 3: 文件系统同步

-   [ ] **文件重命名/移动监听**
    -   [ ] 接入 API: `app.vault.on('rename', callback)`。
    -   [ ] **关联检查**：
        -   当源文件重命名时，检查 `.catdata` 下是否存在旧文件名的元数据。
    -   [ ] **用户交互**：
        -   若存在关联元数据，弹窗提示：“检测到源文件重命名，是否同步重命名翻译数据？”。
        -   确认后执行元数据文件重命名操作。

### Phase 4: 翻译界面渲染

-   [ ] **双栏视图开发**
    -   [ ] 读取 `.cat.md` 中的 JSON 数据。
    -   [ ] 渲染左侧（原文）与右侧（译文编辑框）。
    -   [ ] 绑定数据更新：修改译文时实时更新内存中的 JSON 对象。

### Phase 5: 导出功能

-   [ ] **文档重组**
    -   [ ] 遍历 Block 和 Segment。
    -   [ ] 拼接 `Prefix` + `Translation` (如果 Status 为已翻译) 或 `Source` (Fallback)。
    -   [ ] 导出为新的 Markdown 文件或覆盖原文件（需二次确认）。

### Phase 6: 算法优化与 API (Optional)

-   [ ] 优化分割算法（支持更复杂的标点、不仅限于句号）。
-   [ ] 接入翻译 API (OpenAI/DeepL) 实现预翻译。

### Phase 7: 项目管理系统 (Optional)

-   [ ] **项目视图**
    -   [ ] 顶部入口：打开“项目管理”看板。
    -   [ ] 列表展示：查看所有已创建的翻译项目状态。
-   [ ] **上下文操作**
    -   [ ] 右键文件夹菜单：“在此新建翻译项目”。
    -   [ ] 批量导入 Markdown 生成 Metadata。
-   [ ] **全流程集成**
    -   [ ] 导入 -> 渲染界面 -> 编辑 -> 导出。

### Phase 8: 术语库 (Optional)

-   [ ] 实现术语库管理界面，支持导入、修改、导出术语。
-   [ ] 向 deepl 发送请求时使用术语库
-   [ ] 在当前原文区匹配术语，显示高亮

# 📝 备注

1. 实现监听文件重命名和移动 app.vault.on('rename', callback)，如果发现了重命名事件，则查询理论上的 metadata 文件夹存不存在，有没有对应的 cat.md 文件。如果有则弹窗提示是否对应重命名
2. 读取 metadata 渲染双栏界面。
3. 实现导出功能，
4. 再写详细的分割算法和接入 api（可选）。
5. 实现项目管理（可选）。右键文件夹新建翻译项目。点击顶部按钮打开项目界面查看已有项目。在这里，可以对已有项目进行查看和编辑。可以导入 markdown 文件生成翻译 metadata、读取 metadata 渲染成翻译界面、导出翻译好的译文。
